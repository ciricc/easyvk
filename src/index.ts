import VK from './vk';
import Plugin from './structures/plugin/plugin';
import { GROUP_AUTH_TYPE, USER_AUTH_TYPE, APP_AUTH_TYPE } from './plugins/auth';

export {
  VK,
  Plugin,
  GROUP_AUTH_TYPE,
  USER_AUTH_TYPE,
  APP_AUTH_TYPE
}

export default VK;