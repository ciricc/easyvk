import Plugin from "../../structures/plugin/plugin";

class Auth extends Plugin {
    public name = "auth";
    public options = {

    }

    async onEnable (options) {
        // return this.init();
    }
}

export default Auth;